const { prefix } = require('./config.json')

//['ping','test']
module.exports = {
    name:'reactionoutput',
    description:'does something based on reaction in dm',
    execute(message, args, Discord, client){
        const yes = ':regional_indicator_y:';
        const no = ':regional_indicator_n:';
    }


}